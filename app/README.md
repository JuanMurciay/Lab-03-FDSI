# Aplicación y API

La aplicación ejecutable permanece en la raíz: `index.html`, `styles.css`,
`web.js`, `web.py`, `app.py` y `schema.sql`. Esta carpeta delimita el componente
para la estructura de la guía sin mantener copias divergentes del código.
