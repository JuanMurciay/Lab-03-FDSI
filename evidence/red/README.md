# Red Team — pendiente de autorización

Añadir aquí `start.txt`, `nmap_port80.*`, `curl_home.txt`,
`curl_headers.txt`, inventario observado y exportación ZAP pasiva.
Cada resultado debe indicar fecha UTC, origen, IP/URL autorizada,
responsable, hipótesis, interpretación y referencia a retest. Nunca ejecutar
contra una IP no asignada por el docente.
