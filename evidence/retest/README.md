# Retest — pendiente de la ronda Red/Blue

Repetir exactamente las mismas pruebas autorizadas tras el cambio, guardar
timestamps, headers, resultado de `nginx -t`, ruta oculta y comparación
antes/después. No etiquetar `lab-3` antes de cerrar estas evidencias.
